# payment-provider

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build payment-provider` to build the library.

## Running unit tests

Run `nx test payment-provider` to execute the unit tests via [Jest](https://jestjs.io).
